# Copy to ".env" only if you need a fixed API origin (e.g. production preview).
# For "docker compose" or "npm run dev" with the Vite proxy, leave VITE_API_URL unset.
# VITE_API_URL=http://localhost:8000
